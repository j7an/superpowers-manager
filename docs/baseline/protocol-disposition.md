# Protocol Retirement Disposition

Every behavior ID whose traceability row names a protocol suite has exactly one
row here, recording whether PR 11.5 retires it with the protocol or remaps it to
a surviving home.

The gate is `tests/baseline/protocol-disposition.test.js`. Its input is a frozen
29-ID literal in that file, never a query over `traceability.md` — a
selector-derived input would empty exactly when PR 11.5 deletes the suites, and
the gate would pass vacuously against the event it exists to guard.

`Target` names a **file**, not a `PATH::SELECTOR`. The selector is PR 11.5's to
write; `TRACEABILITY-TESTS-01` resolves it once it exists.

The discriminator is not which suite owns the ID today. For each row, the
contract text in `behavioral-inventory.md` either describes the **transport** —
the serialized protocol-v1 envelope, the response document, and the validator
that reads it — or a **domain rule merely expressed over** that transport.
Transport retires; a domain rule remaps. Where the honest remap target is a file
that does not exist yet, the row retires and PR 11.5 re-establishes the coverage
under a new ID, because a remap to a speculative file would reintroduce exactly
the vacuity this gate exists to prevent.

| Behavior ID | Disposition | Owning suite | Target | Rationale |
|---|---|---|---|---|
| `ADAPTER-CONTROLLED-FAILURE-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract: "A valid controlled failure replays validated messages, error, and hints, yields no result, and returns validator status 1." A controlled failure is a domain state: the adapter must surface a structured error with its hints and yield no partial result. Only the validator-status clause is transport; `runAdapter` still owes the rest. |
| `ADAPTER-ENVELOPE-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Empty, malformed, non-object, and extra-key response input is rejected before replay." Validation of a response document as input, enforced by the response validator (`tests/fixtures/protocol/core/validate-adapter-response.py`), whose `validate_envelope` rejects the non-object and extra-key shapes after `main` refuses input that will not parse. PR-3.3 of this sequence deletes that validator. |
| `ADAPTER-ENVELOPE-KEYS-01` | `retire` | `tests/test_adapter_protocol.sh` | — | Contract: "A response missing a required protocol-v1 envelope key is rejected before result creation or message replay." Pure wire format. Dies with PR 11.5's deletion of the serialized envelope. |
| `ADAPTER-ENVELOPE-TYPES-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Protocol, operation, Boolean, message, result-view, and invocation-view mismatches are rejected before replay." Every term names a protocol-v1 envelope field; this is the envelope's type schema. Dies with PR 11.5's deletion of the serialized envelope. |
| `ADAPTER-FINGERPRINT-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract: "Fingerprint inspection accepts null and exact 7- or 40-hex fingerprints in its exact result shape." Null-or-7/40-hex is a domain vocabulary shared with the reader matrix's Codex installed-commit profile; the 7-or-40-hex half is produced by `codexMetadataCommit` and `manifestShortSha` (`src/codex-state.ts`), reached through the fingerprint view in `runInspect`, which produces the `null` itself when no plugin version is active. |
| `ADAPTER-FINGERPRINT-REJECT-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract: "Fingerprint inspection rejects a non-null fingerprint whose length is neither 7 nor 40 hexadecimal characters." The rejection side of the same domain vocabulary: a wrong-length fingerprint must never be reported as evidence, whatever carries it. |
| `ADAPTER-INSTALL-REJECT-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Install results reject unknown hint keys and empty hint strings." The rejection was the response validator's. In-process the hints are string literals in a returned object (`verification_hints` in `runInstall`, `src/adapter.ts`) and nothing rejects an unknown key or an empty hint string, so no in-process subject remains. Re-adjudicated 2026-08-15; the original `remap` rationale named the construct that *builds* the hints, not one that rejects them. |
| `ADAPTER-INSTALL-RESULT-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract (as adjudicated, before the 2026-08-15 narrowing): "Install success accepts exact `verification_hints` with neither, either, or both optional `mismatch` and `missing` terminal-facing strings." Which hints install may report, and the rule that selects between them, is a domain concern rather than wire format; `runInstall` still emits them via the `verification_hints` result (`src/adapter.ts`), and `src/lifecycle.ts` still consumes them. Narrowed 2026-08-15: `runInstall` emits `missing` unconditionally and `mismatch` exactly when the refresh mode is `add-only`, so two of the four combinations the response schema admitted are reachable in-process. |
| `ADAPTER-OWNERSHIP-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract: "Ownership inspection accepts all internally consistent manager/legacy resource Boolean combinations and derived identity states." The `identity_state` derivation in `runInspect` (`src/adapter.ts`) — from the manager and legacy resource Booleans — is domain logic that outlives the envelope carrying it. |
| `ADAPTER-OWNERSHIP-REJECT-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Ownership inspection rejects old, malformed, wrong-type, and internally inconsistent result shapes." The old and malformed clauses are wire residue. The wrong-type clause has a narrower in-process analogue on the uninstall path — `verifyUninstalledResources` (`src/lifecycle.ts`) refuses a non-Boolean at `resources.plugin` or `resources.marketplace` — but that guards uninstall verification, not ownership inspection, so it is not this contract's subject. The remaining clause cannot be tested in-process: `identity_state` is *derived* from the same resource Booleans it reports (`runInspect`, `src/adapter.ts`), so an internally inconsistent result cannot be produced and therefore cannot be rejected. Any test would duplicate `ADAPTER-OWNERSHIP-01`. Re-adjudicated 2026-08-15. |
| `ADAPTER-PROTOCOL-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Successful `build` and `uninstall` protocol-1 responses accept only their exact empty result objects." The subject is the protocol-1 response and its per-operation result schema, and the value it constrains is empty, so no domain rule remains once acceptance stops happening. Dies with PR 11.5's deletion of the serialized envelope. |
| `ADAPTER-READER-BYTES-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "The response file limit is inclusive at exactly 1,048,576 bytes and rejects the next byte before replay." The response file is the wire artifact, and `MAX_RESPONSE_BYTES` exists only in the response validator (`tests/fixtures/protocol/core/validate-adapter-response.py`), which PR-3.3 of this sequence deletes. No surviving reader profile carries a byte cap, so the honest remap target does not exist and the tie-breaker applies. |
| `ADAPTER-READER-CONSTANTS-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Adapter response JSON rejects all non-standard numeric constants before replay." Scoped to the adapter response document; the profile lives only in `reject_constant` (`tests/fixtures/protocol/core/validate-adapter-response.py`), which PR-3.3 of this sequence deletes, and no surviving reader profile reads a response. Tie-breaker applies. |
| `ADAPTER-READER-DEPTH-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Adapter response JSON accepts exactly 64 nested containers and rejects 65 before replay." `MAX_NESTING = 64` exists only in the response validator (`tests/fixtures/protocol/core/validate-adapter-response.py`), where `nesting_exceeds_limit` enforces it; every surviving strict-JSON profile in `src/` uses 256 or no cap. PR-3.3 of this sequence deletes that validator; tie-breaker applies. |
| `ADAPTER-READER-DUPLICATES-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Adapter response JSON rejects duplicate keys recursively before replay." Only `reject_duplicate_keys` (`tests/fixtures/protocol/core/validate-adapter-response.py`) applies a rejecting duplicate-key policy to a response document, and PR-3.3 of this sequence deletes it. Tie-breaker applies. |
| `ADAPTER-READER-UTF8-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Adapter response size is counted in UTF-8 bytes, not Unicode code points." A refinement of the response-file byte cap, and it retires for the same reason: the cap lives only in `MAX_RESPONSE_BYTES` (`tests/fixtures/protocol/core/validate-adapter-response.py`), which PR-3.3 of this sequence deletes. Tie-breaker applies. |
| `ADAPTER-REPLAY-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Only a completely validated response replays messages, in array order and to each declared stream." Its distinctive content is the validate-before-replay gate over a response document; order and stream retention are owned independently by `DIAG-ADAPTER-01`. The gate is `validate_envelope` (`tests/fixtures/protocol/core/validate-adapter-response.py`), which PR-3.3 of this sequence deletes. Re-examined 2026-08-15: PR-2 hoists a terminal-safety check above the replay's message loop, which preserves a validate-before-write *ordering* but not this contract — that gate validated a parsed response document against a schema, and no such document exists in-process. The ordering is owned by `ADAPTER-TERMINAL-01` and `ADAPTER-SURROGATE-01`, not here. |
| `ADAPTER-STATUS-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "`ok`, adapter exit status, `result`, and `error` obey the success/failure cross-rules." Every term is a protocol-v1 envelope key or the subprocess exit status that accompanies it. Dies with PR 11.5's deletion of the serialized envelope. |
| `ADAPTER-SURROGATE-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter-result.test.js` | Contract: "Terminal-facing protocol strings reject surrogate code points without leaking a traceback." Terminal safety is a property of what reaches the operator, not of the carrier, and the contract is retained in full — all five terminal-facing populations, including the install verification hint — because no single construct replaces `serializeEnvelope`'s one enforcement point. Three do: `writeAdapterFailure` (`src/adapter-protocol.ts`, renamed `src/adapter-result.ts` in PR-4) refuses the error strings without leaking a traceback; `AdapterMessageLog` escapes message text on ingress; and `verifyInstalledFingerprint` (`src/lifecycle.ts`) omits an unsafe verification hint. Witnessed in `tests/unit/adapter-result.test.js` (the first two) and `tests/unit/lifecycle.test.js` (the third). |
| `ADAPTER-TERMINAL-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter-result.test.js` | Contract: "Terminal-facing protocol strings reject C0, DEL, and C1 controls." Terminal-injection safety is a domain rule expressed over the protocol, not a property of it, and the contract is retained in full — all five terminal-facing populations, including the install verification hint — because no single construct replaces `serializeEnvelope`'s one enforcement point. Three do: `writeAdapterFailure` (`src/adapter-protocol.ts`, renamed `src/adapter-result.ts` in PR-4) refuses control characters in the error strings; `AdapterMessageLog` escapes message text on ingress; and `verifyInstalledFingerprint` (`src/lifecycle.ts`) omits an unsafe verification hint. The identical guard, `hasTerminalControl` (`src/codex-json.ts`), already exists off the protocol. Witnessed in `tests/unit/adapter-result.test.js` (the first two) and `tests/unit/lifecycle.test.js` (the third). |
| `ADAPTER-TERMINAL-SHAPE-01` | `retire` | `tests/test_adapter_protocol.py` | — | Contract: "Empty replayed message text is rejected." A record-shape invariant on the envelope's `messages` array, enforced by `serializeEnvelope`'s empty-message-text branch (`src/adapter-protocol.ts`). Unlike a control character, an empty record carries no operator-visible hazard, so nothing survives the carrier. Dies with slice 5's retirement of the serialized transport; the serializer itself is removed in the slice's final pull request. |
| `ADAPTER-UPDATE-CONTROL-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/lifecycle.test.js` | Contract: "Update-control inspection accepts only `managed` or `unsupported` in its exact result shape." The contract splits. The **recognition** rule — two known values, a third rejected — survives in `requireManagedUpdateControl` (`src/lifecycle.ts`) and remaps there. The **reportability** property — that an inspection can emit `unsupported` — retires with the transport: the update-control view returns the literal `managed` (`runInspect`, `src/adapter.ts`), and the old witness ran a fixture shell adapter emitting a canned envelope. Re-adjudicated 2026-08-15; the original rationale said the adapter "still answers with one of the two", which is true but vacuous. |
| `CLI-ENV-CODEX-LISTING-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/baseline/cli-parity.test.js` | Contract: "Codex adapter fingerprint listing uses the executable override; when unset, it resolves `codex` from `PATH`." An environment-override rule expressed over the protocol, not a property of it; survives the transport as Codex executable resolution in `runInspect` (`src/adapter.ts`). |
| `CLI-ENV-CODEX-MUTATION-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/baseline/cli-parity.test.js` | Contract: "Codex adapter install mutation uses the explicit executable override." The same override rule on the mutating path, enforced by the `SUPERPOWERS_CODEX` override in `runInstall` (`src/adapter.ts`) and independent of how the result travels back. |
| `CLI-ENV-INSTALLED-DEFAULTS-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/baseline/cli-parity.test.js` | Contract: "Without explicit overrides, Codex adapter fingerprint listing uses `codex` from `PATH` and installed fingerprint lookup uses `$HOME/.codex`." Default resolution when the overrides are absent, covering Codex executable resolution and the installed fingerprint search root, both in `runInspect` (`src/adapter.ts`); a rule about the environment, not the envelope. |
| `CLI-ENV-INSTALLED-ROOT-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/baseline/cli-parity.test.js` | Contract: "Codex fingerprint inspection; the active version selects the exact plugin cache path below this root." Which cache path the active version selects is a filesystem-resolution rule — `installedRootForVersion` (`src/codex-state.ts`) — that the protocol only reports. |
| `CLI-ENV-REFRESH-MODE-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/baseline/cli-parity.test.js` | Contract: "Codex adapter install; allowed values are `add-only` and `remove-add`." The `add-only` / `remove-add` enumeration in `runInstall` (`src/adapter.ts`) is validated before any Codex mutation; the rejection of a third value is domain policy, not wire format. |
| `DIAG-ADAPTER-01` | `remap` | `tests/test_adapter_protocol.py` | `tests/unit/adapter.test.js` | Contract: "Validated adapter messages, controlled errors, and hints retain their declared stream and array order." What the operator sees, on which stream, in what order, is a diagnostics contract; the tests "the adapter replays the validator success line as one stdout record" and "the adapter replays a multi-error failure as one record per line" (`tests/unit/adapter.test.js`) already assert it directly over `runAdapter`. |
| `PROV-READER-CODEX-SOURCE-01` | `remap` | `tests/test_adapter_protocol.sh` | `tests/unit/provenance.test.js` | Contract: "The Codex build source-reader profile is frozen exactly as the matrix states." A row of the reader and trust-boundary matrix, whose four sibling provenance profiles already live outside the protocol; implemented by `readCodexBuildSource` (`src/provenance.ts`) and already exercised by the test "PROV-READER-CODEX-SOURCE-01 Codex build source reader preserves its accepting profile" (`tests/unit/provenance.test.js`). |
