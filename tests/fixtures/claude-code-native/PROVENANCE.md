# Claude Code native fixture

Hand-written inert test input, not copied from upstream. `plugin.json.txt`
mirrors the shape of upstream `.claude-plugin/plugin.json` (name `superpowers`,
semver `version`); `hooks.json.txt` declares no hooks. The skill and license
files are reused from `tests/fixtures/pi-native/`.
