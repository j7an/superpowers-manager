name: Dependency Safety

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write
  statuses: write
  issues: write

concurrency:
  group: dependency-safety-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  safety:
    # Real dependency-safety scan runs for Dependabot only; every other PR
    # gets the status-only gate in dependency-safety-non-bot-gate.yml.
    # `pull_request.user.login` is the spoofing-resistant actor field.
    if: github.event.pull_request.user.login == 'dependabot[bot]'
    # Unreleased candidate proposed by the held shared-workflows release run;
    # release approval remains pending.
    uses: j7an/shared-workflows/.github/workflows/dependency-safety.yml@8aafcda9817cace8993dd7d356457bda549e5f74 # v4.6.0
    with:
      auto_merge: true
      release_age_policy: advisory
      minimum_release_age_days: 5
