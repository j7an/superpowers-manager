name: Dependency Safety Non-Bot Gate

on:
  pull_request_target: # zizmor: ignore[dangerous-triggers] status-only path; never checks out or runs PR code
    types: [opened, synchronize, reopened]
    branches: [main]

permissions: {}

concurrency:
  group: dep-safety-gate-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  gate:
    permissions:
      statuses: write
    # Unreleased candidate proposed by the held shared-workflows release run;
    # release approval remains pending.
    uses: j7an/shared-workflows/.github/workflows/dependency-safety-non-bot-gate.yml@8aafcda9817cace8993dd7d356457bda549e5f74 # v4.6.0
